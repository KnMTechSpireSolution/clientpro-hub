# Commerce Core Restore Fix

Fixed sidebar routing: added pages.commerce route handler so Commerce Core opens the active Commerce Core menu instead of falling back to generic Module accessible screen.
