# Commerce Invoice Fix

Fixed line item input focus issue. Quotation template no longer shows Payment Terms. Invoice/Proforma Invoice added. Bank/payment settings and invoice-specific terms added. Receipt invoice reference now displays invoice number instead of internal ID.
